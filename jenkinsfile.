pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello dims'
            }
        }
                stage('build') {
            steps {
                echo 'build dims'
            }
        }
                stage('test') {
            steps {
                echo 'test dims'
            }
        }
                stage('deploy') {
            steps {
                echo 'deploy dims'
            }
        }
    }
}
